/**
 * 环境变量枚举
 */
export enum envKeys {
  /**
   * 服务器地址
   */
  SERVER_HOST = 'SERVER_HOST',
  /**
   * 服务器端口
   */
  SERVER_PORT = 'SERVER_PORT',
  /**
   * 上传文件夹
   */
  UPLOAD_DIR = 'UPLOAD_DIR',
}
